@model ForgotPasswordViewModel
<div class="row justify-content-center mt-3">
    <div class="col-md-8 col-lg-6">
        <partial name="_Message" />
        <h4 class="text-center mb-3">@ViewData["Title"]</h4>
        <div class="card">
            <div class="card-body">
                <p class="text-secondary">Ingresa tu correo y te enviaremos un enlace para restablecer la contraseña.</p>
                <form asp-action="ForgotPassword" method="post">
                    <div class="mb-3">
                        <label class="form-label" asp-for="Email">Correo electrónico</label>
                        <input class="form-control" type="email" asp-for="Email" autocomplete="email">
                        <span class="text-danger small" asp-validation-for="Email"></span>
                    </div>
                    <button class="btn btn-primary w-100" type="submit">Enviar enlace</button>
                </form>
            </div>
        </div>
    </div>
</div>
